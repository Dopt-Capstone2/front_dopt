<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <ImageView
        android:id="@+id/prefer_kind_img"
        android:layout_width="350dp"
        android:layout_height="350dp"
        android:src="@drawable/shelter_join"
        android:layout_margin="40dp"
        android:scaleType="fitCenter"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/shelter_name_tx"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="보호소 이름 입력하기"
        android:textColor="@color/purple_500"
        android:textStyle="bold"
        android:textSize="14sp"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="@id/join_nickname_jo"
        app:layout_constraintTop_toBottomOf="@id/prefer_kind_img"/>


    <EditText
        android:id="@+id/join_nickname_jo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="50dp"
        android:padding="15dp"
        android:hint="보호소 이름 입력하기"
        android:background="@drawable/purple_border_radius"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/shelter_name_tx"/>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/start_shelter_btn"
        android:text="Dopt 시작하기"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/purple500_all_radius"
        android:textColor="@color/white"
        android:layout_marginTop="20dp"
        android:layout_marginHorizontal="50dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/join_nickname_jo"/>

</androidx.constraintlayout.widget.ConstraintLayout>